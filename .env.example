# Backend environment example
# Copy to .env and adjust values
PORT=3000
NODE_ENV=development

# MongoDB Atlas Connection (REQUIRED)
# Get this from: MongoDB Atlas → Connect → Drivers
# Format: mongodb+srv://username:password@cluster.mongodb.net/keyvora?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/keyvora?retryWrites=true&w=majority

# Comma-separated list of allowed frontend origins for CORS
FRONTEND_ORIGIN=http://localhost:5173

# Replace with a strong random key in production (run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
ENCRYPTION_KEY=your_64_char_hex_key_here
